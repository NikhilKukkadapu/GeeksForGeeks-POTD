class Solution {
    public boolean isBitSet(int n) {
        // code here
         if(n == 0) {
            return false;
        }
        
        // Check if n+1 is power of 2
        return ( (n & (n + 1)) == 0 );
    }
}
